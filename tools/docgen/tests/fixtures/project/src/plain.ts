/**
 * A file header separated from the declaration below by a blank line —
 * it belongs to the module and must not be read as the declaration's
 * doc comment.
 */

export function plain(): void {
  // The body is beside the point; the header above is.
}
